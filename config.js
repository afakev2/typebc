// ============================================
// ملف إعدادات البوت - عدل القيم هنا حسب رغبتك
// ============================================

module.exports = {
    // الكلمة التي سيرسلها البوت (عدلها هنا)
    BOT_WORD: "-كت",

    // وقت إعادة الإرسال بالثواني
    RESEND_TIME_SECONDS: 1800, // 1800 ثانية = 30 دقيقة
    
    // إعدادات الإيقاف التلقائي للحماية
    AUTO_STOP: {
        ENABLED: true,              // تشغيل/إيقاف ميزة الحماية
        WORK_TIME_MINUTES: 60,       // مدة العمل قبل الإيقاف (بالدقائق)
        REST_TIME_MINUTES: 80,      // مدة الإيقاف (بالدقائق) 180 = 3 ساعات
    },

    // معرفات الديسكورد (تأخذ من Render env)
    // لا تغيرها هنا - ستأتي من متغيرات البيئة
    SERVER_ID: null,
    ORDERS_CHANNEL_ID: null,
    TOKEN: null
};

// ============================================
// شرح تفصيلي للإعدادات:
// ============================================

/*
🔧 BOT_WORD:
   - الكلمة التي سيرسلها البوت تلقائياً
   - مثال: "تم استلام الطلب" أو "تم التنفيذ"
   - يرسلها البوت كل فترة محددة

⏱️ RESEND_TIME_SECONDS:
   - الوقت بين كل إرسال وآخر (بالثواني)
   - 60 = دقيقة واحدة
   - 300 = 5 دقائق
   - 1800 = 30 دقيقة
   - 3600 = ساعة كاملة

🛡️ AUTO_STOP:
   - ENABLED: true (تشغيل) / false (إيقاف)
   - WORK_TIME_MINUTES: كم دقيقة يعمل البوت
   - REST_TIME_MINUTES: كم دقيقة يتوقف (للتخفي)
   
   مثال ضبط:
   - يعمل ساعة (60 دقيقة) 
   - يتوقف 3 ساعات (180 دقيقة)
   - يعيد الكرة تلقائياً

📝 ملاحظة:
   المعرفات (SERVER_ID, ORDERS_CHANNEL_ID, TOKEN)
   تضعها في Render Environment Variables
   وليس في هذا الملف
*/
